# Choropleth-style question → use a horizontal bar chart by Province/region (no map geometry).
# Example: counts or mean price per province.
fig, ax = plt.subplots(figsize=(10, max(6, df['Province'].nunique() * 0.22)))
agg = df.groupby('Province', as_index=False)['<METRIC_COL>'].mean().sort_values('<METRIC_COL>', ascending=True)
sns.barplot(data=agg, y='Province', x='<METRIC_COL>', ax=ax, orient='h')
ax.set_xlabel('...')
ax.set_ylabel('Tỉnh/Thành phố')
ax.set_title('...')
plt.xticks(rotation=0)
plt.tight_layout()
